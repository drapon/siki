# TASK-0010: loop運用パターンの文書化

**タスクID**: TASK-0010
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 3 - 指揮者による子生成 + 運用ガイド
**信頼性レベル**: 🔵 *requirements.md REQ-024, REQ-025「運用ガイド、コード変更ほぼ無し」より*

## 関連文書

- **概要**: [overview.md](overview.md)
- **要件定義**: [../../spec/conductor-hierarchy/requirements.md](../../spec/conductor-hierarchy/requirements.md) - REQ-024, REQ-025
- **ユーザストーリー**: [../../spec/conductor-hierarchy/user-stories.md](../../spec/conductor-hierarchy/user-stories.md) - エピック1〜3（実装指揮・レビュー応答指揮・レビュー指揮）
- **アーキテクチャ**: [../../design/conductor-hierarchy/architecture.md](../../design/conductor-hierarchy/architecture.md)
- **データフロー**: [../../design/conductor-hierarchy/dataflow.md](../../design/conductor-hierarchy/dataflow.md)
- **MCPツール仕様**: [../../design/conductor-hierarchy/mcp-tools.md](../../design/conductor-hierarchy/mcp-tools.md)

## タスク概要

Phase 0〜3で実装した `dispatch`（worktree/subtree）・`move_worktree`・`spawn_child_worktree`・`list_sessions(scope=children)` の各機構は、REQ-024/REQ-025が求める「指揮者worktreeが `/loop` で反応的に子を監視しつつdispatchで動かし、ワーカーworktreeはdispatchで起き、完了後は何もせずidleに戻る」という運用パターンをすでにコードレベルで実現できる状態にある。本タスクはコード変更を一切伴わず、README.md（またはdocs/配下の新規ガイド）にこの運用パターンを文書化するだけの DIRECT タスクである。

具体的には、user-stories.mdが定義する3つのユースケース（実装指揮／レビュー応答指揮／レビュー指揮）を、README.mdの既存構造に自然に収まる形で説明し、あわせて新規MCPツール3種と `list_sessions` の `scope=children` 拡張をREADME.mdの「Available Tools」表に追記する。

## 依存タスク

- **前提タスク**: [TASK-0002](TASK-0002.md)（dispatch配送パイプラインが動作していること）、[TASK-0009](TASK-0009.md)（`spawn_child_worktree` が実装されていること）
- **後続タスク**: なし（Phase 3最終タスク）

## 完了条件

- [ ] README.mdの「Available Tools」表（現状 `list_sessions`/`send_message`/`broadcast`/`set_summary`/`handoff`/`get_context` の6行）に、`dispatch`・`move_worktree`・`spawn_child_worktree` の3行が追加されている
- [ ] `list_sessions` の説明文に `scope=children`（指揮者が自身の子孫worktreeのセッションのみに絞る）についての追記がある
- [ ] README.mdに「指揮者パターン」相当の新セクションが追加され、以下3ユースケースをそれぞれ説明している
  - 実装指揮（エピック1）
  - レビュー応答指揮（エピック2）
  - レビュー指揮（エピック3）
- [ ] 当該セクションが「指揮者側は `/loop` で反応的にポーリングし `dispatch` で次の指示を出す」「ワーカー側はdispatchで起きてタスクを終えたらidleに戻るだけでよく、`/loop` を自分で回す必要がない（トークン消費ゼロで待機、REQ-025）」という役割分担を明示している
- [ ] 本タスクでソースコード（`src/`配下）に一切変更がない（DIRECTタスクとしての完了条件）

## 主要実装項目

### 1. README.md「Available Tools」表への追記 🔵

**信頼性**: 🔵 *README.md現状（155-168行目）・mcp-tools.mdの3ツール仕様より*

現状のREADME.md「MCP Server」節「Available Tools」表:

```markdown
| Tool | Description |
|------|-------------|
| `list_sessions` | List all active sessions with summaries and pending messages |
| `send_message` | Send a message to a specific session, worktree, or project |
| `broadcast` | Broadcast a message to all sessions (or scoped to project/worktree) |
| `set_summary` | Update the current session's work summary |
| `handoff` | Hand off context to another session (includes git state) |
| `get_context` | Fetch context from another session or worktree |
```

以下のように3行追加し、`list_sessions` の説明文を更新する（既存6行の順序・文体を維持し、末尾に新規3行を追加する形を推奨）:

```markdown
| Tool | Description |
|------|-------------|
| `list_sessions` | List all active sessions with summaries and pending messages (scope: `machine`/`project`/`worktree`/`children` — `children` limits results to the caller's descendant worktrees) |
| `send_message` | Send a message to a specific session, worktree, or project |
| `broadcast` | Broadcast a message to all sessions (or scoped to project/worktree) |
| `set_summary` | Update the current session's work summary |
| `handoff` | Hand off context to another session (includes git state) |
| `get_context` | Fetch context from another session or worktree |
| `dispatch` | Auto-inject a prompt into a worktree's (or subtree's) tab0 Claude terminal, no human approval step |
| `move_worktree` | Re-parent a worktree under a different conductor worktree (or detach it) |
| `spawn_child_worktree` | Create a new child worktree under a conductor worktree and set its parent link |
</markdown>
```

**注記** 🟡: 上記スキーマ・文言はmcp-tools.mdの説明文を要約したもの。実装（TASK-0001〜0009）で最終確定したツール名・パラメータと齟齬がないか、本タスク着手時に実装済みコード（`src/mcp/protocol.rs`）と突き合わせて確認すること。

### 2. 「指揮者パターン」セクションの新設 🔵

**信頼性**: 🔵 *user-stories.md エピック1〜3、requirements.md REQ-024/REQ-025より*

README.mdの「MCP Server」節の直後（または「Available Tools」表の直後）に新セクションを追加する。既存の見出しレベル（`##`/`###`）に合わせ、以下の骨子で記述する:

```markdown
## Conductor Pattern

A worktree becomes a "conductor" simply by having child worktrees attached to it via
`move_worktree` or `spawn_child_worktree` — there's no separate "conductor" entity to
create. Any worktree can act as a conductor for zero or more children, and children can
be re-parented between conductors at any time.

The division of responsibility:

- **Conductor side**: runs `/loop` to reactively poll children (via `list_sessions`
  with `scope: "children"`, or by checking PR comments), and issues the next
  instruction to a child (or to the whole subtree) via `dispatch`.
- **Worker side**: does *not* need to run `/loop`. A worker wakes up when `dispatch`
  auto-injects a prompt into its tab0 terminal, does the work, and returns to idle —
  costing zero tokens while waiting for the next dispatch (REQ-025).

### Use case 1: Implementation conducting

The conductor Claude tracks the implementation progress of each child worktree while
the actual coding happens inside the children. The conductor dispatches implementation
tasks to idle children and checks on progress via `list_sessions({scope: "children"})`.

### Use case 2: Review-response conducting

The conductor runs `/loop`, reactively polling for new PR review comments. When a
comment arrives, the conductor dispatches a fix instruction to the worktree responsible
for that PR; that worktree makes the fix and returns to idle.

### Use case 3: Review conducting

The conductor runs a review loop while treating review targets as child worktrees
(created via `spawn_child_worktree`). The conductor dispatches review instructions to
the whole subtree at once (`dispatch({target: {type: "subtree", id: "<self>"}, ...})`),
and each child reviews its assigned target independently.
```

**実装時の注意** 🟡: 上記英語ドキュメントの文体はREADME.md既存部分（英語）に合わせている。プロジェクトの言語方針上、日本語での追記が適切と判断される場合は、既存セクションの言語（README.mdは全体が英語）と整合させること。

### 3. コード変更なしの確認 🔵

**信頼性**: 🔵 *DIRECTタスクの定義（requirements.md「運用ガイド、コード変更ほぼ無し」）より*

本タスクは `README.md`（および必要であれば `docs/` 配下の追加ガイド）以外のファイルを変更しない。`src/`配下のファイルへの変更が発生していないことを、`git diff --stat` 等で確認すること。

## 確認項目

DIRECTタスクのためユニットテスト・統合テストは対象外。代わりに以下を確認する。

- [ ] README.md「Available Tools」表に新規3行（`dispatch`, `move_worktree`, `spawn_child_worktree`）が追加され、Markdown表として崩れずレンダリングされること
- [ ] `list_sessions` の説明文が `scope=children` を含む形に更新されていること
- [ ] 「指揮者パターン」相当のセクションが、実装指揮・レビュー応答指揮・レビュー指揮の3ユースケースそれぞれについて具体的な説明を含んでいること
- [ ] セクション内に「指揮者側は`/loop`＋`dispatch`」「ワーカー側はdispatchで起きてidleに戻るだけ（`/loop`不要、トークン消費ゼロ）」という役割分担の説明が明記されていること
- [ ] `src/`配下に差分がないこと（`git diff --stat src/` が空であること）
- [ ] README.md追記箇所のMarkdown構文が壊れていないこと（見出しレベル・表の整合性を目視確認）

## 実装手順（DIRECT）

1. `/tsumiki:direct-setup TASK-0010` — README.mdへの「Available Tools」表追記・「指揮者パターン」セクション追記を実施する
2. `/tsumiki:direct-verify TASK-0010` — 上記「確認項目」に従い、Markdownとしての整合性・記載内容の網羅性・`src/`配下への変更がないことを確認する

## 信頼性レベルサマリー

- 総項目数: 3項目
- 🔵 青信号: 2件 (67%、README.md現状構造・user-stories.mdのユースケース・REQ-024/025の役割分担はいずれも確定資料に基づく)
- 🟡 黄信号: 1件 (33%、新規3ツールの正確な名称・パラメータ・戻り値の文言はTASK-0001〜0009の実装確定後に突き合わせ確認が必要)
- 🔴 赤信号: 0件 (0%)

**品質評価**: 高品質（本タスクはコード変更を伴わない文書化タスクであり、前提となるTASK-0001〜0009がrequirements.md/mcp-tools.mdの記述通りに実装されていれば、内容の齟齬リスクは低い）
