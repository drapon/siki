# TASK-0004: cmd_rm / cmd_path / cmd_list（非対話パス）

**タスクID**: TASK-0004
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - CLI 実装
**信頼性レベル**: 🔵 *requirements.md REQ-005/006/007・git::remove_worktree(git.rs:190)・既存 list(main.rs:93)*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/siki-cli-commands/requirements.md) - REQ-005,006,007
- **設計文書**: [🔄 dataflow.md](../../design/siki-cli-commands/dataflow.md)（rm / path / list）

## タスク概要

軽量な3コマンドの非対話パスをまとめて実装する。
- `cmd_rm`: `git::WorktreeManager::remove_worktree(project_path, wt_path)`
- `cmd_path`: `config::worktree_path` を絶対パスで stdout 出力（不在は stderr + 非0）
- `cmd_list`: 既存 list ロジックを cli へ移し、任意の project 名で絞り込み

## 依存タスク

- **前提タスク**: [TASK-0002](TASK-0002.md)
- **後続タスク**: [TASK-0007](TASK-0007.md)（rm の対話補完・削除確認を接続）

## 完了条件

- [ ] `siki rm <project> <name>` で worktree が削除される
- [ ] `siki path <project> <name>` が絶対パスを stdout に出す（不在時は非0終了）
- [ ] `siki list [project]` が任意プロジェクト絞り込みに対応
- [ ] 単体テスト green（Red-Green 検証済み）/ `cargo test` exit 0

---

## 主要実装項目

### cmd_rm 🔵
**信頼性**: 🔵 *git.rs:190 / archive フロー(main.rs:4570) より*。非対話パスでは確認なしで削除（確認は TASK-0007 で対話時のみ付与）。

### cmd_path 🔵
**信頼性**: 🔵 *REQ-006。stdout はパスのみ（選択 UI を出さない）*

### cmd_list 🔵
**信頼性**: 🔵 *既存 list(main.rs:93-105) の移設 + project フィルタ*

---

## 基本的なテスト要件（TDD）

### TC-1: rm で削除 🔵
tempfile git リポジトリで worktree 作成 → cmd_rm → `git worktree list` から消える

### TC-2: path 出力 🔵
`worktree_path` の文字列が stdout に一致（不在時は非0）

### TC-3: list フィルタ 🟡
複数プロジェクト相当のデータで project 指定時に1件のみ列挙

**テストファイル**: 各コマンドの `#[cfg(test)]`

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements TASK-0004` → testcases → red → green → refactor → verify-complete

---

## 信頼性レベルサマリー

- 総項目数: 6 / 🔵 5 (83%) / 🟡 1 (17%) / 🔴 0
- **品質評価**: 高品質
