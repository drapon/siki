# TASK-0001: CLI モジュール土台と resolve_base_branch 移設

**タスクID**: TASK-0001
**タスクタイプ**: DIRECT
**推定工数**: 2時間
**フェーズ**: Phase 1 - CLI 実装
**信頼性レベル**: 🔵 *architecture.md「モジュール構成」より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/siki-cli-commands/requirements.md) - NFR(保守性)
- **設計文書**: [📐 architecture.md](../../design/siki-cli-commands/architecture.md)

## タスク概要

CLI 層の土台を用意する。`src/cli/` モジュールの空枠を作り、`main.rs` に `mod cli;` を宣言。
`main.rs:5330` の private `resolve_base_branch` を `config.rs` に `pub fn` として移設し、
既存の呼び出し9箇所（main.rs:690,1196,1506,2577,2745,2800,2965,5085,5289）を `config::resolve_base_branch` に置換する。

## 依存タスク

- **前提タスク**: なし（起点）
- **後続タスク**: [TASK-0002](TASK-0002.md), [TASK-0006](TASK-0006.md)

## 完了条件

- [ ] `src/cli/mod.rs` / `args.rs` / `prompt.rs` の空枠（`pub fn` シグネチャのみ）が存在し `mod cli;` 宣言済み
- [ ] `config::resolve_base_branch(project_path, project_name)` が存在し、解決順（siki.json > config.toml > "origin/main"）は不変
- [ ] main.rs の旧呼び出しが全て config 版に置換され、旧 private 関数は削除
- [ ] `cargo build` が exit 0

---

## 主要実装項目

### resolve_base_branch の移設 🔵

**信頼性**: 🔵 *main.rs:5330 の既存実装をそのまま config へ移すだけ*

`config.rs` へ移設。`config::load_config` / `load_effective_siki_json` は同モジュール内参照になる。

### cli モジュール枠 🔵

**信頼性**: 🔵 *interfaces.rs のシグネチャ草案より*

`src/cli/mod.rs` に `cmd_new/cmd_rm/cmd_path/cmd_list/cmd_run` の枠、`args.rs`/`prompt.rs` のシグネチャを置く（中身は後続タスク）。

---

## 基本的なテスト要件

### ビルド回帰 🔵

**信頼性**: 🔵 *verification.md（build は exit 0 を確認）より*

- `cargo build` が通る。既存の `resolve_base_branch` 関連テスト（config.rs のテスト）が壊れないこと。

## 実装手順（DIRECT）

1. `/tsumiki:direct-setup` - モジュール枠作成・関数移設・呼び出し置換
2. `/tsumiki:direct-verify` - `cargo build` / 既存テストで回帰確認

---

## 信頼性レベルサマリー

- 総項目数: 4 / 🔵 4 (100%) / 🟡 0 / 🔴 0
- **品質評価**: 高品質
