# TASK-0007: handle_remove_project_confirm_keyへの配線

**タスクID**: TASK-0007
**タスクタイプ**: TDD
**推定工数**: 1.5時間
**フェーズ**: Phase 3 - プロジェクト削除時のreindex追加（Bug C）
**信頼性レベル**: 🔵 *architecture.md「Reindex」・requirements.md REQ-102/103より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/worktree-deletion-index-fix/requirements.md) - REQ-102, REQ-103
- **設計文書**: [📐 architecture.md](../../design/worktree-deletion-index-fix/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/worktree-deletion-index-fix/dataflow.md) - 「プロジェクト削除フロー（Bug C 解消）」

## タスク概要

TASK-0006で新設した `reindex_project_maps` を `handle_remove_project_confirm_key`
（`main.rs:4417`）に配線する。`app.projects.remove(pi)`（`main.rs:4460`）の直後、既存の
`selected_worktree` 補正（`main.rs:4463-4469`）と並べて呼び出す。

## 依存タスク

- **前提タスク**: [TASK-0006](TASK-0006.md)
- **後続タスク**: [TASK-0008](TASK-0008.md)（統合確認）

## 完了条件

- [ ] `handle_remove_project_confirm_key` 内、`app.projects.remove(pi)` の直後に
      `reindex_project_maps(sessions, terminals, claude_terms, pi)` を呼び出す
- [ ] 呼び出し順序は「削除対象プロジェクトのクリーンアップ→Vec::remove→
      reindex_project_maps→selected_worktree補正」とする
- [ ] 3プロジェクト構成で中間のプロジェクトを削除した際、後続プロジェクトの
      `sessions`/`terminals`/`claude_terms` が正しくシフトされることを確認する
      統合的なユニットテストを追加する（実PTYは使わずHashMapを手動構築）
- [ ] 既存の `handle_remove_project_confirm_key` 関連の挙動（クリーンアップ・
      `selected_worktree`補正・project.json削除）が回帰しないことを確認する
- [ ] `cargo test` が通る

---

## 実装詳細

### handle_remove_project_confirm_key への配線 🔵

**信頼性**: 🔵 *architecture.md「Reindex」・コード調査(main.rs:4417-4483)より*

**実装ファイル**: `src/main.rs:4417-4483`

```rust
// 変更箇所（main.rs:4460 の直後）:
app.projects.remove(pi);

// 新規追加: プロジェクト削除によるreindex（REQ-102）
reindex_project_maps(sessions, terminals, claude_terms, pi);

// 既存: selected_worktree をリセット（変更なし、main.rs:4463-4469）
if let Some((sel_pi, sel_wi)) = app.selected_worktree {
    if sel_pi == pi {
        app.selected_worktree = None;
    } else if sel_pi > pi {
        app.selected_worktree = Some((sel_pi - 1, sel_wi));
    }
}
```

---

## 単体テスト要件

### テストケース1: 中間プロジェクト削除で後続プロジェクトの各マップが正しくシフトする 🔵

**信頼性**: 🔵 *requirements.md TC-101-01・ユーザーストーリー2.1より*

**Given**: 3プロジェクト構成を模した `sessions`/`terminals`/`claude_terms`
（`project_index=0,1,2` のエントリを含む）を手動構築し、`handle_remove_project_confirm_key`
相当のロジックを `project_index=1` を対象に実行する
**When**: 削除確定処理を実行する
**Then**: 元 `project_index=2` のエントリはすべて `project_index=1` のキーへ移動している

**テストファイル**: `src/main.rs`（既存 `mod tests` 内に追加）

### テストケース2: selected_worktreeの補正と併存して問題ない 🔵

**信頼性**: 🔵 *既存実装 (`main.rs:4463-4469`) の回帰防止より*

**Given**: `app.selected_worktree = Some((2, 0))` の状態で `project_index=1` を削除する
**When**: `handle_remove_project_confirm_key` 相当の処理を実行する
**Then**: `app.selected_worktree` が `Some((1, 0))` に補正され、かつ対応する
`sessions`/`terminals`/`claude_terms` のエントリも同じ `(1, 0)` 系のキーに揃っている

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements TASK-0007`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

## 注意事項

- 呼び出し順序（Vec::remove → reindex → selected_worktree補正）を誤ると、reindex前の
  古いインデックスを前提にした補正になり不整合を起こすため注意する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 2 | 0 | 0 | 2 |

**総項目数**: 3項目 / 🔵 3 (100%) / 🟡 0 / 🔴 0
**品質評価**: 高品質
