# TASK-0002: terminals用resolve_terminal_key新設と配線

**タスクID**: TASK-0002
**タスクタイプ**: TDD
**推定工数**: 3時間
**フェーズ**: Phase 1 - PTY/Claudeタブのid解決強化（Bug A）
**信頼性レベル**: 🔵 *architecture.md「対象コンポーネント」・requirements.md REQ-005より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/worktree-deletion-index-fix/requirements.md) - REQ-003, REQ-005
- **設計文書**: [📐 architecture.md](../../design/worktree-deletion-index-fix/architecture.md)
- **型定義**: [📝 interfaces.rs](../../design/worktree-deletion-index-fix/interfaces.rs)

## タスク概要

通常ターミナル（`terminals`）は `claude_terms` と異なり、`TerminalOutput`/`TerminalExited`
処理（`main.rs:747-751,772-776`）で直接キー引きのみ行っており、id ベースの再探索処理が
存在しない。`resolve_claude_term_key` と同型の `resolve_terminal_key` を新設し、両イベント
ハンドラの直接キー引きをこのヘルパー経由に置き換える。

## 依存タスク

- **前提タスク**: なし（起点。TASK-0001と並行実行可）
- **後続タスク**: [TASK-0008](TASK-0008.md)（統合確認）

## 完了条件

- [ ] `resolve_terminal_key` を `main.rs` に新設する（直接キー引き→id全体走査の順）
- [ ] `AppEvent::TerminalOutput` 処理（`main.rs:747-751`）を `resolve_terminal_key` 経由に
      書き換える
- [ ] `AppEvent::TerminalExited` 処理（`main.rs:772-776`）を `resolve_terminal_key` 経由に
      書き換える
- [ ] worktree_idシフト後もterminal_id一致で発見できることを検証するユニットテストを追加する
- [ ] 一致するidが存在しない場合に黙って何もしない（drop）ことを検証するユニットテストを追加する
- [ ] `cargo test` が通る

---

## 実装詳細

### resolve_terminal_key 新設 🔵

**信頼性**: 🔵 *architecture.md「データモデル（Rust型定義）」より*

**実装ファイル**: `src/main.rs`

```rust
/// terminals 用の id ベース再解決ヘルパー（resolve_claude_term_key と対になる新設関数）
/// REQ-005: 現状 terminals には再探索処理が存在せず、直接キー引きのみだったギャップを埋める
fn resolve_terminal_key(
    terminals: &HashMap<TerminalKey, terminal::TerminalEmulator>,
    worktree_id: app::WorktreeId,
    tab_index: usize,
    terminal_id: u64,
) -> Option<TerminalKey> {
    if let Some(emu) = terminals.get(&(worktree_id, tab_index)) {
        if emu.id() == terminal_id {
            return Some((worktree_id, tab_index));
        }
    }
    terminals
        .iter()
        .find(|(_, emu)| emu.id() == terminal_id)
        .map(|(key, _)| *key)
}
```

### イベントハンドラの書き換え 🔵

**信頼性**: 🔵 *dataflow.md「修正後のフロー（Bug A 解消）」より*

**実装ファイル**: `src/main.rs:747-751,772-776`

```rust
// 変更前:
// } else if let Some(emu) = terminals.get_mut(&(worktree_id, tab_index)) {
//     if emu.id() == terminal_id {
//         emu.process(&data);
//     }
// }

// 変更後:
} else if let Some(key) = resolve_terminal_key(terminals, worktree_id, tab_index, terminal_id) {
    if let Some(emu) = terminals.get_mut(&key) {
        emu.process(&data); // TerminalExited側は emu.mark_exited() に置き換え
    }
}
```

---

## 単体テスト要件

### テストケース1: worktree_idシフト後もterminal_id一致で発見できる 🔵

**信頼性**: 🔵 *requirements.md TC-001-02より*

**Given**: `terminals` に `(pi, 2)` キーで `id=55` の `TerminalEmulator` が存在する
（worktree削除reindexで移動済みという想定）
**When**: `resolve_terminal_key(terminals, worktree_id=(pi,3), tab_index=0, terminal_id=55)`
を呼ぶ
**Then**: `Some((pi,2), 0)` が返る

**テストファイル**: `src/main.rs`（既存 `mod tests` 内に追加）

### テストケース2: 一致するidが存在しない場合はNone 🔵

**信頼性**: 🔵 *requirements.md REQ-401・TC-001-E01より*

**Given**: `terminals` に `terminal_id=99` のエントリが存在しない
**When**: `resolve_terminal_key(terminals, worktree_id, tab_index, 99)` を呼ぶ
**Then**: `None` が返り、呼び出し側のイベント処理で `.process()`/`.mark_exited()` が
呼ばれない

### テストケース3: 直接キー引きが成功する通常ケース（高速パス） 🔵

**信頼性**: 🔵 *architecture.md「パフォーマンス」（高速パスの維持）より*

**Given**: reindexが発生しておらず `terminals` の `(worktree_id, tab_index)` に
`terminal_id` が一致するエントリが存在する
**When**: `resolve_terminal_key` を呼ぶ
**Then**: 全体走査を経ずに `Some((worktree_id, tab_index))` が返る（挙動として区別は不要だが、
戻り値が正しいことを確認する）

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements TASK-0002`
2. `/tsumiki:tdd-testcases`
3. `/tsumiki:tdd-red`
4. `/tsumiki:tdd-green`
5. `/tsumiki:tdd-refactor`
6. `/tsumiki:tdd-verify-complete`

## 注意事項

- `TerminalExited` 側は `emu.process(&data)` ではなく `emu.mark_exited()` を呼ぶ点に注意
  （既存実装 `main.rs:772-776` を参照）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 3 | 0 | 0 | 3 |

**総項目数**: 5項目 / 🔵 5 (100%) / 🟡 0 / 🔴 0
**品質評価**: 高品質
