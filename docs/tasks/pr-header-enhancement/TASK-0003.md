# TASK-0003: AppEvent::PrInfo の配線変更（送受信4箇所）

**タスクID**: TASK-0003
**タスクタイプ**: TDD
**推定工数**: 2時間
**フェーズ**: Phase 2 - 取得・イベント層
**信頼性レベル**: 🔵 *architecture.md「イベント」・既存 event.rs:41 より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/pr-header-enhancement/requirements.md) - REQ-001〜003
- **設計文書**: [📐 architecture.md](../../design/pr-header-enhancement/architecture.md), [🔄 dataflow.md](../../design/pr-header-enhancement/dataflow.md)

## タスク概要

`AppEvent::PrInfo` のペイロードを `title: Option<String>` から `info: Option<PrInfo>` に変更し、
送信3箇所・受信1箇所を新ペイロードへ配線し直す。これで取得→状態更新の経路が `PrInfo` で繋がる。

## 依存タスク

- **前提タスク**: [TASK-0001](TASK-0001.md), [TASK-0002](TASK-0002.md)
- **後続タスク**: [TASK-0004](TASK-0004.md)

## 完了条件

- [ ] `event.rs:41` の `PrInfo { worktree_id, title }` を `{ worktree_id, info: Option<crate::app::PrInfo> }` に変更
- [ ] 送信3箇所（`main.rs:249` 起動時 / `702` セッション完了後 / `2161` worktree追加時）を `fetch_pr_info` + `info:` に更新
- [ ] 受信ハンドラ（`main.rs:1406`）を `wt.pr = info;` に更新
- [ ] `cargo build` が通る

---

## 主要実装項目

### イベント型の変更 🔵

**信頼性**: 🔵 *architecture.md イベント節より*

```rust
PrInfo { worktree_id: WorktreeId, info: Option<crate::app::PrInfo> },
```

**実装ファイル**: `src/event.rs`

### 送受信の配線 🔵

**信頼性**: 🔵 *既存呼び出し箇所（main.rs:249/702/1406/2161）より*

- 送信: `fetch_pr_title(...)` → `fetch_pr_info(...)`、`title` → `info` に置換（3箇所）
- 受信: `wt.pr_title = title;` → `wt.pr = info;`（1箇所）

**実装ファイル**: `src/main.rs`

---

## 基本的なテスト要件

### 配線整合 🔵

**信頼性**: 🔵 *dataflow.md フロー1 より*

- [ ] `cargo build` で型不整合エラーがないこと（送受信が `Option<PrInfo>` で一致）
- [ ] 既存の起動/セッション完了/worktree追加の各経路で `wt.pr` が更新されること（手動またはログで確認）

---

## 実装手順（TDD）

1. `/tsumiki:tdd-requirements TASK-0003` 2. `tdd-testcases` 3. `tdd-red` 4. `tdd-green` 5. `tdd-refactor` 6. `tdd-verify-complete`

## 信頼性レベルサマリー

- 総項目数: 2実装 + 2テスト / 🔵 4 (100%) / 🟡 0 / 🔴 0
- **品質評価**: 高品質
